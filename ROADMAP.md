# Roadmap

Build order from the master spec, kept as-is because it's sound: one milestone at a time,
test and update `TASKS.md`/`CHANGELOG.md` at the end of each.

| # | Milestone | Status | Owner |
|---|---|---|---|
| 1 | System inspection | **Done — 2026-08-09** | You, via local Claude Code |
| 2 | Architecture + repository | Done (stack decision made from real data) | Cowork |
| 3 | Basic Jarvis desktop UI | **Done, verified 2026-08-09** — frontend builds (`npm run build`), and the local Claude Code session compiled the Tauri backend for real: `apps/desktop/backend/target/debug/jarvis` is an actual Mach-O arm64 binary. It corrected two things in Cowork's unverified scaffold (a redundant `[lib]` block in `Cargo.toml`, and relative frontend paths in `tauri.conf.json`) and generated real icons in `apps/desktop/backend/icons/` (source still needs replacing — see Milestone 4 note below) | Cowork (frontend) + local Claude Code (Rust build) |
| 4 | Theme system + Jarvis Core animation | Frontend part done — 4 themes as CSS tokens (`src/theme.css`), animated `JarvisCore` component with all 10 states, `prefers-reduced-motion` respected, theme persists via localStorage. Checked `apps/desktop/backend/icons/icon.png`: it's a distinct blue holographic circular design, not the Marvel/Iron Man GitHub asset flagged earlier — fine to keep | Cowork |
| 5 | Command engine | **Done, verified 2026-08-09** — real parser + executor in `apps/desktop/frontend/src/commandEngine.ts`, 5 unit tests passing (`npm run test`), wired to the (now enabled) command bar. Only 2 real actions exist so far (theme switching, status), everything else honestly returns "not implemented yet" instead of pretending | Cowork |
| 6 | Obsidian memory | **Done, verified 2026-08-09** — real `Inbox/`/`Daily/`/`Notes/`/`Briefs/`/`System/` structure built in `~/Documents/Obsidian Vault`, `JARVIS.md`, `System/memory.md`, and all 3 scripts (`what_open.py`, `connect_this.py`, `orphan_scan.py`) written and run successfully against real seeded content | Cowork |
| 7 | Supabase | **Code done, project not provisioned, 2026-08-09** — real client (`src/lib/supabaseClient.ts`), real store implementation (`supabaseStore.ts`), real migration (`packages/database/migrations/0001_init.sql`). Cannot create the actual Supabase project (Cowork doesn't create accounts) — see `INSTALLATION.md` for the exact steps you need to run | Cowork (code) / you (provisioning) |
| 8 | Projects + tasks | **Done, verified 2026-08-09** — real CRUD UI (`ProjectsView`, `TasksView`), backed by a local-storage adapter that implements the same interface Supabase will (`getStore()` switches automatically once M7's project exists). Sidebar nav now actually routes between views instead of always showing the dashboard. 11 tests passing, build verified | Cowork |
| 9 | Voice + wake word | **Partially done, 2026-08-09.** Stack decided (openWakeWord + faster-whisper, local; ElevenLabs, cloud, behind an interface — see `VOICE_SETUP.md`). 3 Python scripts written in `~/Documents/Obsidian Vault/System/voice/` (syntax-checked, **not run** — no mic in the sandbox that wrote them). Frontend voice settings UI built and verified (`npm test`/`build` pass): real device enumeration, real permission status where supported, persisted toggles. Nothing wires the Python scripts to the Tauri app yet | Cowork |
| 10 | Claude / agent orchestration | **Architecture only, 2026-08-09** — design is written in `AGENT_SYSTEM.md`: one orchestrator (JARVIS) routes requests to specialists (research/debug/planning/docs), Level 3 gating applies regardless of which agent asks. Not code yet because there's no local runtime process to host it in — this needs the local Claude Code / Agent SDK session (per `ARCHITECTURE.md`'s Cowork-vs-local split) to actually be the orchestrator, and building routing logic against a runtime that doesn't exist yet would be exactly the "renders but nothing's connected" trap the spec warns against (principle #6) | — |
| 11 | Claude Code project development mode | **Architecture only, 2026-08-09** — the "Continue Project X" workflow (spec §62: load context → check roadmap/tasks → inspect repo → plan → implement → test → update docs → report) is documented in `AGENT_SYSTEM.md`. This is genuinely just what a local Claude Code session already does when given that instruction directly — Milestone 11's real work is wiring it as a JARVIS command (`"continue project X"` → invoke local Claude Code in that repo with that prompt) rather than inventing new capability, which needs the M10 orchestration process to exist first | — |
| 12 | GitHub | **Architecture only, 2026-08-09** — plan in `MCP_SETUP.md`: read access by default, push/create/merge gated as Level 3. `SYSTEM_INSPECTION_REPORT.md` confirmed GitHub auth exists locally over HTTPS (not SSH) — so a local `gh`/git-based integration is plausible without new OAuth. Not built because Cowork has no access to your local git credentials to test against, and building it unverified would violate the same principle M12's own test plan enforces (`TESTING.md`: "using a safe test repo, no push without explicit approval") | — |
| 13 | Automations (launchd, not n8n) | **Script done + verified, job written but not activated, 2026-08-09** — `~/Documents/Obsidian Vault/System/scripts/morning_brief.py` is real: run manually against the actual vault, produced a correct `Briefs/2026-08-09.md` from real Daily/Notes/Inbox/memory content, explicitly stating what it doesn't cover (calendar/email). `packages/automations/launchd/dev.leonardo.jarvis.morningbrief.plist` schedules it for 7am daily — written and documented (`packages/automations/launchd/README.md`) but deliberately not loaded; that's your call to make. Also corrected `AUTOMATIONS.md`, which previously and incorrectly claimed a Cowork scheduled task already did this | Cowork |
| 14 | Calendar | **Architecture only, 2026-08-09.** Correction to earlier entries: this Cowork session actually has a Calendar MCP connector already available (list/search/create/update/respond-to events) — it's connected at the Cowork-account level, not something you still need to authorize there. But that access doesn't carry into the standalone JARVIS desktop app: Cowork's connectors are session-scoped (see `MCP_SETUP.md`'s first line — JARVIS's MCP servers get configured in the *local* Claude Code runtime, separately). So the real Milestone 14 work is configuring an equivalent calendar MCP server in your local Claude Code / Agent SDK config, not something Cowork can hand off automatically. Not done here because it's local config, and because reading/writing your actual calendar isn't something to do unprompted just to test an architecture note | — |
| 15 | Email | **Architecture only, 2026-08-09.** Same correction as Calendar: a Gmail-like MCP connector (search/read/draft/label) is available in this Cowork session already, but again doesn't transfer to the desktop app's local runtime — needs its own MCP config locally, same as M14. Read access should stay default-on for JARVIS's own use (per `MCP_SETUP.md`), sends/drafts gated Level 3. Not exercised here for the same reason as M14: no unprompted reading of real email just to prove the architecture note true | — |
| 16 | Web research | **Architecture only, 2026-08-09** — Cowork can already do ad hoc web research today (this session used `web_fetch`/search while building JARVIS's own docs). The gap isn't capability, it's exposure: nothing in the JARVIS desktop app can trigger a research task and get a written-up result back into a project or the vault. Real M16 work is a command (`"research X"`) that invokes the local orchestrator (M10) with search/fetch tools and writes findings to `Notes/` or a project — blocked on M10 existing first, not on research capability itself | — |
| 17 | Specialist agents | **Architecture only, 2026-08-09** — table of specialist roles already sketched in `AGENT_SYSTEM.md` (research/debug/planning/docs). This is the same underlying gap as M10: specialists are a routing decision on top of an orchestrator process that doesn't exist yet. Building specialist prompts/personas now, before there's a runtime to route between them, would be exactly the "half-built feature nobody can use" pattern the spec's principle #20 warns against | — |
| 18 | Permissions + security | **Done, verified 2026-08-09** — `src/permissions.ts` classifies command kinds into Level 1/2/3 per spec §54. Level 3 (`delete-project`) is enforced with a real `ApprovalDialog` (ACTION/CONTEXT/REASON/RISK per spec §55) and `useApproval` promise-based hook — nothing deletes until the user clicks Approve, every time, no auto-approve. Wired into `ProjectsView`'s Delete button (the actual gated action) and recognized (not executed) by the typed command bar, which honestly redirects to the UI instead of faking a confirmation flow in text. 16 tests passing, build verified | Cowork |
| 19 | Testing + performance | **Partially real, 2026-08-09.** Not "not started" — every milestone shipped so far (5, 6, 7/8, 9, 13, 18) already has real automated or manual verification behind it: 16 passing frontend unit tests (`npm run test`), `tsc -b`/`vite build` checked after every change, `morning_brief.py` run against real vault content rather than just syntax-checked, the Tauri backend actually compiled to a Mach-O binary. What's missing is what `TESTING.md` still lists as unrun: M9's voice scripts (no mic in this sandbox), M12/14/15/16/17's tests (nothing to test — those milestones are architecture-only), and any performance benchmarking (app startup time, command latency) — meaningless before M3's visual launch is confirmed by you. So: the *discipline* is already in place, the *coverage* has real gaps tied to specific blockers above, not neglect | — |
| 20 | V1 polish | **Not started, correctly** — this is explicitly last in the build order because polishing an app with 9 of 20 milestones still architecture-only or blocked on your action (Supabase provisioning, Rust visual launch confirmation, voice hardware, GitHub/Calendar/Gmail local MCP config, launchd activation) would mean polishing surfaces that might still change. Revisit once M1–M18's blockers above are cleared by you and M10/M11/M12/M16/M17 have moved from architecture to code | — |

## Sequencing note

Milestones 1–2 are done (2026-08-09). Stack is decided: Tauri, Supabase Cloud, no n8n initially
(launchd/cron scripts instead), local Claude Code as orchestrator, vault at
`~/Documents/Obsidian Vault` — see `ARCHITECTURE.md`. Before Milestone 3 starts: relocate this
repo out of the Claude session artifacts folder to somewhere durable (e.g. `~/Developer/jarvis`),
and recheck disk space (~22 GiB free at inspection time) since the Rust toolchain install is
next.
