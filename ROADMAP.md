# Roadmap

Build order from the master spec, kept as-is because it's sound: one milestone at a time,
test and update `TASKS.md`/`CHANGELOG.md` at the end of each.

| # | Milestone | Status | Owner |
|---|---|---|---|
| 1 | System inspection | **Done — 2026-08-09** | You, via local Claude Code |
| 2 | Architecture + repository | Done (stack decision made from real data) | Cowork |
| 3 | Basic Jarvis desktop UI | **Done, launch confirmed 2026-08-09** — frontend builds (`npm run build`), and `cargo tauri dev` was run for real: Vite served the frontend on `:1420`, the Rust backend compiled in 2.79s, and `target/debug/jarvis` launched and came to the foreground (confirmed via process list). It corrected two things in Cowork's unverified scaffold (a redundant `[lib]` block in `Cargo.toml`, and relative frontend paths in `tauri.conf.json`) and generated real icons in `apps/desktop/backend/icons/` (source still needs replacing — see Milestone 4 note below) | Cowork (frontend) + local Claude Code (Rust build + launch) |
| 4 | Theme system + Jarvis Core animation | Frontend part done — 4 themes as CSS tokens (`src/theme.css`), animated `JarvisCore` component with all 10 states, `prefers-reduced-motion` respected, theme persists via localStorage. Checked `apps/desktop/backend/icons/icon.png`: it's a distinct blue holographic circular design, not the Marvel/Iron Man GitHub asset flagged earlier — fine to keep | Cowork |
| 5 | Command engine | **Done, verified 2026-08-09** — real parser + executor in `apps/desktop/frontend/src/commandEngine.ts`, 5 unit tests passing (`npm run test`), wired to the (now enabled) command bar. Only 2 real actions exist so far (theme switching, status), everything else honestly returns "not implemented yet" instead of pretending | Cowork |
| 6 | Obsidian memory | **Done, verified 2026-08-09** — real `Inbox/`/`Daily/`/`Notes/`/`Briefs/`/`System/` structure built in `~/Documents/Obsidian Vault`, `JARVIS.md`, `System/memory.md`, and all 3 scripts (`what_open.py`, `connect_this.py`, `orphan_scan.py`) written and run successfully against real seeded content | Cowork |
| 7 | Supabase | **Backend confirmed live, 2026-08-10; UI click-through still needs you.** `cargo tauri dev` relaunched clean (killed a stale leftover Vite process holding :1420 first) — frontend served on :1420, Rust compiled and `target/debug/jarvis` came up with no errors. Sent a REST call to `https://nriarfrgmjsygswlweed.supabase.co/rest/v1/projects` using the exact insert shape `SupabaseStore.createProject` issues (same anon key `.env.local` has, same table, same RLS policy): insert succeeded, read-back confirmed the row, delete cleaned it up — proves schema + RLS + anon key all work end-to-end, and `getStore()` will resolve to `SupabaseStore` since `isSupabaseConfigured()` is true. What's NOT verified: an actual click of "Add" in the live window — this sandboxed session has no WindowServer/screen-recording access (same limitation noted at Milestone 3: process launches fine, but this session can't see or screenshot its own window). `cargo tauri dev` was left running — open the window and type a project name into Projects to confirm visually, then check the `projects` table in the Supabase dashboard | Cowork + Leonardo (account/provisioning) + local Claude Code (backend verification) |
| 8 | Projects + tasks | **Done, verified 2026-08-09** — real CRUD UI (`ProjectsView`, `TasksView`), backed by a local-storage adapter that implements the same interface Supabase will (`getStore()` switches automatically once M7's project exists). Sidebar nav now actually routes between views instead of always showing the dashboard. 11 tests passing, build verified | Cowork |
| 9 | Voice + wake word | **Done, all three scripts confirmed live, 2026-08-10.** `wake_listener.py`: detected "Hey Jarvis" live (score 0.92) after two real bugs found and fixed (openWakeWord's tflite backend has no reliable Apple Silicon wheel — switched to `inference_framework="onnx"` + `onnxruntime`; and the model weight files aren't bundled in the pip package — added an automatic `download_models()` call). `transcribe.py`: confirmed working end-to-end by Leonardo (mic → faster-whisper `small` → correct transcript). `speak_daemon.py`: confirmed — Leonardo heard real audio out loud. Went through an API key mixup (Key ID vs actual key), then a stale-process-still-using-old-key bug, then discovered ElevenLabs' free API tier blocks TTS generation entirely regardless of voice (402 `paid_plan_required` on both a Voice Library voice and a premade voice) — so `speak_daemon.py` now supports two engines via `config.json`'s `tts_engine`, defaulting to macOS's built-in `say` (free, offline, no key). `elevenlabs` engine remains available for once the plan is upgraded. Frontend voice settings UI built and verified (`npm test`/`build` pass): real device enumeration, real permission status where supported, persisted toggles. **Wired into Tauri, 2026-08-10** — `apps/desktop/backend/src/voice.rs` spawns a new `listen_loop.py` (combines wake+transcribe into one continuous process) and `speak_daemon.py` as child processes, forwarding parsed events to the frontend, which feeds transcripts into the same `commandEngine.ts` the typed command bar uses. 7 new Rust unit tests (parsing only — no mic needed), `cargo tauri dev` hot-reloaded clean, 31 frontend tests / `tsc -b` / `vite build` all still pass. **Mechanically confirmed live end-to-end, 2026-08-10, one more round of fixes to reverify** — Leonardo launched `cargo tauri dev` from his own Terminal (not the assistant's sandbox, which genuinely cannot access CoreAudio — confirmed, not assumed) and said "Hey Jarvis" for real: wake word detected, recorded, transcribed, routed through `commandEngine.ts`, and spoken back via `say`. That proves the full pipeline is mechanically wired correctly. Found and fixed three real bugs along the way: a missing `capabilities/default.json` blocking `event.listen()` entirely (fixed, reverified); openWakeWord returning numpy `float32` scores that `json.dumps()` can't serialize (fixed, reverified — the wake event that surfaced this next bug proves it); and, surfaced by that same test, a wake-phrase-inclusive transcript ("hey jarvis status") not matching any command pattern, compounded by a React StrictMode double-listener bug that spoke every response twice. Both fixed and unit-tested (10 Rust + 4 frontend) but **not yet re-confirmed live** — next real step is Leonardo trying "Hey Jarvis, status" again | Cowork + Leonardo (live testing) + local Claude Code (Tauri wiring + live debugging) |
| 10 | Claude / agent orchestration | **Core piece done, 4 commands routed through it, 2026-08-09.** Scoped with Leonardo before building: Tauri's Rust backend shells out to the local `claude` CLI (`claude -p --output-format json`) rather than standing up a separate Agent SDK server — reuses the already-authenticated CLI and its already-verified local MCP servers with zero new credential handling. Verified by hand first (`claude -p`, `claude --bg`, `claude agents --json` all tested directly from a terminal before any Rust was written). `orchestrator.rs`'s `run_orchestrator` is the single Tauri command every one of `research`/`continue-project`/`check-calendar`/`check-email` calls with a different prompt — no per-command Rust code needed, just `commandEngine.ts` additions. 3 Rust + 18 frontend command-engine tests passing; `cargo tauri dev` hot-reloaded every change cleanly. **Background-mode path built, 2026-08-10** — `continue-project` now uses `run_orchestrator_background` (`claude --bg`) + `poll_orchestrator_background` (`claude agents --json --all`, polled every 5s) instead of blocking the command bar; the other four commands stay synchronous (quick reads, no benefit from polling). Getting the actual result text needed a real design decision: `agents --json` never exposes it and `claude logs` is raw ANSI, so the result is fetched via a `--fork-session` resume that asks the model to repeat its last answer — see `AGENT_SYSTEM.md` for the full writeup and its known cost/fidelity caveat. 10 new Rust unit tests using real captured CLI output as fixtures (20 Rust + 20 frontend command-engine tests total), `cargo tauri dev` hot-reloaded clean. Not yet verified: an actual `continue project <name>` clicked through the live app window — this session has no WindowServer access to its own window (same gap as M7/M9) | Local Claude Code + Cowork |
| 11 | Claude Code project development mode | **Done, verified 2026-08-09** — `continue project <name>` in the command bar sends the orchestrator spec §62's full workflow (load context → inspect repo → decide + implement next step → test → update docs → report) as a single prompt. Level 2 in `permissions.ts`. Reuses `run_orchestrator` unchanged — no new Rust. 1 parsing test + 1 execution test in `commandEngine.test.ts`. Not yet run against a real project inside the live app window (only the mocked-orchestrator automated tests) — see `TASKS.md` | Cowork |
| 12 | GitHub | **Read path done, 2026-08-09.** `gh auth status` verified real, active HTTPS auth (`Leo1511-byte`, scopes `gist`/`read:org`/`repo`/`workflow`). No GitHub MCP server is configured locally, and none was needed — `check my github` / `check my prs` / `check my pull requests` / `check my issues` route through the orchestrator with a prompt that just tells the local `claude` CLI to use the already-authenticated `gh` command directly (it has its own bash/tool access). Level 1 (explicitly read-only: "don't create, comment on, merge, or close anything"). Write actions (create PR, comment, merge) intentionally not built — real Level 3 surface for later | Cowork |
| 13 | Automations (launchd, not n8n) | **Script done + verified, job written but not activated, 2026-08-09** — `~/Documents/Obsidian Vault/System/scripts/morning_brief.py` is real: run manually against the actual vault, produced a correct `Briefs/2026-08-09.md` from real Daily/Notes/Inbox/memory content, explicitly stating what it doesn't cover (calendar/email). `packages/automations/launchd/dev.leonardo.jarvis.morningbrief.plist` schedules it for 7am daily — written and documented (`packages/automations/launchd/README.md`) but deliberately not loaded; that's your call to make. Also corrected `AUTOMATIONS.md`, which previously and incorrectly claimed a Cowork scheduled task already did this | Cowork |
| 14 | Calendar | **Done, wired into the command bar, 2026-08-09.** `list_calendars` verified working from a local Claude Code session (2 real calendars). `check my calendar` / `check calendar` / `what's on my calendar` / `show my calendar` now route through the orchestrator with an explicitly read-only prompt. Level 1 in `permissions.ts`. Not yet run against the live app window — same gap as M11 | Cowork |
| 15 | Email | **Done, wired into the command bar, 2026-08-09.** `list_labels` verified working locally (real Gmail data, e.g. 1040 INBOX messages). `check my email` / `check my inbox` / `check my mail` route through the orchestrator with an explicitly read-only prompt ("don't send, draft, or label anything"). Level 1 in `permissions.ts`. Sends/drafts intentionally not built — that's a real Level 3 surface for a later, separate pass | Cowork |
| 16 | Web research | **Done, first real slice shipped 2026-08-09** (local Claude Code) — `research <topic>` routes through the orchestrator, which writes findings to a new `Notes/` file and replies with a one-sentence summary + path. 3 Rust + 4 frontend tests. Not yet: research results linked back into a specific project (only the vault-wide `Notes/` folder today) | Local Claude Code |
| 17 | Specialist agents | **Reframed and largely done, 2026-08-09** — see `AGENT_SYSTEM.md`'s rewrite: rather than separate agent processes/personas, "specialists" are four purpose-built prompt templates over the one `run_orchestrator` command (`research`, `continue-project`, `check-calendar`, `check-email`), each explicit about what it should and shouldn't do. Simpler than the original routing-table design and equally real. What's not built: the Agents status page (spec §42). The background-mode path (`claude --bg`) it was waiting on now exists (M10, 2026-08-10) — but only one command (`continue-project`) uses it, and the Command Log already surfaces its start/finish, so there's still not enough distinct concurrent background state to justify a dedicated status page. Revisit if more commands move to background mode | Cowork |
| 18 | Permissions + security | **Done, verified 2026-08-09** — `src/permissions.ts` classifies command kinds into Level 1/2/3 per spec §54. Level 3 (`delete-project`) is enforced with a real `ApprovalDialog` (ACTION/CONTEXT/REASON/RISK per spec §55) and `useApproval` promise-based hook — nothing deletes until the user clicks Approve, every time, no auto-approve. Wired into `ProjectsView`'s Delete button (the actual gated action) and recognized (not executed) by the typed command bar, which honestly redirects to the UI instead of faking a confirmation flow in text. 16 tests passing, build verified | Cowork |
| 19 | Testing + performance | **Substantially real, 2026-08-09.** 31 passing frontend unit tests (`npm run test`), `tsc -b`/`vite build` checked after every change, 3 Rust unit tests in `orchestrator.rs` (not independently re-run by Cowork — no `cargo` in this sandbox), `cargo tauri dev` actually launched and hot-reloaded through every change, `morning_brief.py` and 3 vault scripts run against real content, `transcribe.py`/`gh auth`/Calendar MCP/Gmail MCP all verified against real systems. Remaining gaps, specific rather than vague: `wake_listener.py`/`speak_daemon.py` need live runs (mic hardware, and an API key only Leonardo can add); none of the 5 new orchestrator commands (M10/11/12/14/15/16) have been clicked through in the actual running app window yet, only tested with a mocked orchestrator; and performance benchmarking (app startup, command latency) hasn't started — reasonable to defer until the command set stops growing | — |
| 20 | V1 polish | **Still not started, and still correctly so, 2026-08-10** — the only genuinely blocked-on-you item left is M7 (needs your Supabase account). M9 is fully done as of today. M10's background-mode path is the one piece of real backend work still open, needing `cargo` to verify safely. Wiring the three confirmed voice scripts into the Tauri app as sidecar processes is the next real voice step. Everything else in M1–M19 is done or has a real, narrow, named next step. Worth starting once the five orchestrator commands have actually been exercised in the live app window — polishing UI that's never been looked at risks polishing the wrong thing | — |

## Sequencing note

Milestones 1–2 are done (2026-08-09). Stack is decided: Tauri, Supabase Cloud, no n8n initially
(launchd/cron scripts instead), local Claude Code as orchestrator, vault at
`~/Documents/Obsidian Vault` — see `ARCHITECTURE.md`. Before Milestone 3 starts: relocate this
repo out of the Claude session artifacts folder to somewhere durable (e.g. `~/Developer/jarvis`),
and recheck disk space (~22 GiB free at inspection time) since the Rust toolchain install is
next.
