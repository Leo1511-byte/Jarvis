# Roadmap

Build order from the master spec, kept as-is because it's sound: one milestone at a time,
test and update `TASKS.md`/`CHANGELOG.md` at the end of each.

| # | Milestone | Status | Owner |
|---|---|---|---|
| 1 | System inspection | **Done — 2026-08-09** | You, via local Claude Code |
| 2 | Architecture + repository | Done (stack decision made from real data) | Cowork |
| 3 | Basic Jarvis desktop UI | **Done, launch confirmed 2026-08-09** — frontend builds (`npm run build`), and `cargo tauri dev` was run for real: Vite served the frontend on `:1420`, the Rust backend compiled in 2.79s, and `target/debug/jarvis` launched and came to the foreground (confirmed via process list). It corrected two things in Cowork's unverified scaffold (a redundant `[lib]` block in `Cargo.toml`, and relative frontend paths in `tauri.conf.json`) and generated real icons in `apps/desktop/backend/icons/` (source still needs replacing — see Milestone 4 note below) | Cowork (frontend) + local Claude Code (Rust build + launch) |
| 4 | Theme system + Jarvis Core animation | Frontend part done — 4 themes as CSS tokens (`src/theme.css`), animated `JarvisCore` component with all 10 states, `prefers-reduced-motion` respected, theme persists via localStorage. Checked `apps/desktop/backend/icons/icon.png`: it's a distinct blue holographic circular design, not the Marvel/Iron Man GitHub asset flagged earlier — fine to keep | Cowork |
| 5 | Command engine | **Done, extended 2026-08-11** — real parser + executor in `apps/desktop/frontend/src/commandEngine.ts`, wired to the command bar and voice input alike. Grew from 2 real actions (theme switching, status) to 11 (research/continue-project/check-calendar/check-email/check-github/check-memory via the orchestrator, plus a general `ask` fallback added 2026-08-10 after live testing showed ordinary questions got a hardcoded "not implemented yet" instead of ever reaching Claude — everything unmatched now routes through the orchestrator as a direct message instead). 42 unit tests passing (`npm run test`) | Cowork |
| 6 | Obsidian memory | **Done, verified 2026-08-09; reachable from the app itself as of 2026-08-11.** Real `Inbox/`/`Daily/`/`Notes/`/`Briefs/`/`System/` structure built in `~/Documents/Obsidian Vault`, `JARVIS.md`, `System/memory.md`, and all 3 scripts (`what_open.py`, `connect_this.py`, `orphan_scan.py`) written and run successfully against real seeded content. The vault itself was always real, but nothing in the desktop app could reach it — Leonardo flagged this live ("only obsidian isn't linked"). New `check-memory` command (typed, spoken, or a button in the new Memory view) asks the orchestrator's `claude` to read the vault directly, same no-new-Rust pattern as Calendar/Email/GitHub. `StatusPanel`'s Obsidian row moved from a hardcoded "not-wired" to the same dynamic unverified/not-wired logic the other systems use. 2 new commandEngine tests (42 total) | Cowork |
| 7 | Supabase | **Fully confirmed live, 2026-08-10 — UI click-through done.** Created a real project ("Ape War Game") in the Projects view; it persisted and displays with Status/Created/Delete. Combined with the earlier direct-REST proof, `SupabaseStore` is confirmed working end-to-end through the actual UI, not just backend calls. `cargo tauri dev` relaunched clean (killed a stale leftover Vite process holding :1420 first) — frontend served on :1420, Rust compiled and `target/debug/jarvis` came up with no errors. Sent a REST call to `https://nriarfrgmjsygswlweed.supabase.co/rest/v1/projects` using the exact insert shape `SupabaseStore.createProject` issues (same anon key `.env.local` has, same table, same RLS policy): insert succeeded, read-back confirmed the row, delete cleaned it up — proves schema + RLS + anon key all work end-to-end. That backend proof was then confirmed through the real UI too (see above) | Cowork + Leonardo (account/provisioning + live testing) + local Claude Code (backend verification) |
| 8 | Projects + tasks | **Done, verified 2026-08-09** — real CRUD UI (`ProjectsView`, `TasksView`), backed by a local-storage adapter that implements the same interface Supabase will (`getStore()` switches automatically once M7's project exists). Sidebar nav now actually routes between views instead of always showing the dashboard. 11 tests passing, build verified | Cowork |
| 9 | Voice + wake word | **Confirmed live end-to-end, 2026-08-10, five real bugs found and fixed along the way** (see `VOICE_SETUP.md` for the full writeup). `wake_listener.py`/`transcribe.py`/`speak_daemon.py` each individually confirmed, then wired into Tauri (`apps/desktop/backend/src/voice.rs` spawns `listen_loop.py` — a new script combining wake+transcribe into one continuous process — and `speak_daemon.py` as child processes). Bugs found live and fixed: (1) missing `capabilities/default.json` silently blocked `event.listen()` entirely; (2) openWakeWord's numpy `float32` score isn't JSON-serializable; (3) wake-phrase-inclusive transcripts ("hey jarvis status") matched no command pattern; (4) a React StrictMode double-listener spoke every response twice; (5) **feedback loop** — JARVIS's own spoken replies were picked up by the still-open mic and re-triggered itself, fixed with a file-based mute flag (`speaking.flag`) coordinating `listen_loop.py`/`speak_daemon.py`. All five fixed and confirmed. 17 Rust + 35 frontend tests passing. **New gap found and fixed 2026-08-10 (not voice-specific, affects typed input too):** `commandEngine.ts` had no general fallback — anything not matching one of 9 rigid patterns hit a hardcoded "Not implemented yet" and never reached Claude at all, which is why ordinary spoken questions got no real reply. Added an `ask` command kind routing unrecognized input through the orchestrator as a direct message (scoped read-only-by-prompt). **Crash-recovery gap also fixed, 2026-08-10** (found needing `cargo`, handed off between sessions via `HANDOFF_VOICE_CRASH_RECOVERY.md`, then fixed): `listen_loop.py`'s per-cycle work is now wrapped in broad exception handling (logs, reports, backs off 1s, keeps looping) instead of the whole process dying on any unexpected error; `voice.rs` gained a per-process monitor thread that detects an unexpectedly-dead child via `try_wait()` and auto-restarts it with linear backoff, capped at 5 attempts, emitting a new `Status` voice-event so the frontend shows "crashed, restarting" / "reconnected" / "gave up" in the Command Log instead of voice just going silently dark. 22 Rust + 38 frontend tests passing | Cowork + Leonardo (live testing) + local Claude Code (Tauri wiring + live debugging) |
| 10 | Claude / agent orchestration | **Core piece done, 4 commands routed through it, 2026-08-09.** Scoped with Leonardo before building: Tauri's Rust backend shells out to the local `claude` CLI (`claude -p --output-format json`) rather than standing up a separate Agent SDK server — reuses the already-authenticated CLI and its already-verified local MCP servers with zero new credential handling. Verified by hand first (`claude -p`, `claude --bg`, `claude agents --json` all tested directly from a terminal before any Rust was written). `orchestrator.rs`'s `run_orchestrator` is the single Tauri command every one of `research`/`continue-project`/`check-calendar`/`check-email` calls with a different prompt — no per-command Rust code needed, just `commandEngine.ts` additions. 3 Rust + 18 frontend command-engine tests passing; `cargo tauri dev` hot-reloaded every change cleanly. **Background-mode path built, 2026-08-10** — `continue-project` now uses `run_orchestrator_background` (`claude --bg`) + `poll_orchestrator_background` (`claude agents --json --all`, polled every 5s) instead of blocking the command bar; the other four commands stay synchronous (quick reads, no benefit from polling). Getting the actual result text needed a real design decision: `agents --json` never exposes it and `claude logs` is raw ANSI, so the result is fetched via a `--fork-session` resume that asks the model to repeat its last answer — see `AGENT_SYSTEM.md` for the full writeup and its known cost/fidelity caveat. 10 new Rust unit tests using real captured CLI output as fixtures (20 Rust + 20 frontend command-engine tests total), `cargo tauri dev` hot-reloaded clean. Not yet verified: an actual `continue project <name>` clicked through the live app window with the exact phrasing the command needs — every attempt so far ("continue with Ape War Game project") missed the strict pattern and fell through to the `ask` fallback instead (which handled it safely, but doesn't exercise this path). See `TASKS.md` for the exact phrase still needed | Local Claude Code + Cowork |
| 11 | Claude Code project development mode | **Done, verified 2026-08-09** — `continue project <name>` in the command bar sends the orchestrator spec §62's full workflow (load context → inspect repo → decide + implement next step → test → update docs → report) as a single prompt. Level 2 in `permissions.ts`. Reuses `run_orchestrator` unchanged — no new Rust. 1 parsing test + 1 execution test in `commandEngine.test.ts`. Not yet run against a real project inside the live app window (only the mocked-orchestrator automated tests) — see `TASKS.md` | Cowork |
| 12 | GitHub | **Fully confirmed live, 2026-08-11.** First live attempt (with the exact phrase) got an honest permission failure instead of a fake success — `.claude/settings.local.json` only had `gh auth *`/`gh repo *` pre-approved, not the read-only search/list/view commands a headless `claude -p` call needs (no human around to approve a prompt in non-interactive mode). Added narrow grants (`gh search prs/issues`, `gh pr list/status/view`, `gh issue list/view`, `gh notifications`) — deliberately no `gh *` wildcard, so write subcommands stay unapproved. Retested: `check my github` returned a real, correct result ("No open PRs or issues assigned to you... GitHub is clear right now"). `gh auth status` originally verified real, active HTTPS auth (`Leo1511-byte`, scopes `gist`/`read:org`/`repo`/`workflow`). Write actions (create PR, comment, merge) intentionally not built — real Level 3 surface for later | Cowork + Leonardo (live testing) |
| 13 | Automations (launchd, not n8n) | **Script done + verified, job written but not activated, 2026-08-09** — `~/Documents/Obsidian Vault/System/scripts/morning_brief.py` is real: run manually against the actual vault, produced a correct `Briefs/2026-08-09.md` from real Daily/Notes/Inbox/memory content, explicitly stating what it doesn't cover (calendar/email). `packages/automations/launchd/dev.leonardo.jarvis.morningbrief.plist` schedules it for 7am daily — written and documented (`packages/automations/launchd/README.md`) but deliberately not loaded; that's your call to make. Also corrected `AUTOMATIONS.md`, which previously and incorrectly claimed a Cowork scheduled task already did this | Cowork |
| 14 | Calendar | **Fully confirmed live, 2026-08-10.** `check my calendar` returned a real, correct result from the actual app ("completely clear Aug 10–12, nothing to prep for"). Needed one fix along the way: `.claude/settings.local.json` only had `list_calendars` pre-approved, not the event-reading tools the check-calendar prompt actually needs — added `list_events`/`search_events`/`get_event` and it worked. Level 1 in `permissions.ts` | Cowork + Leonardo (live testing) |
| 15 | Email | **Fully confirmed live, 2026-08-10.** `check my email` returned a real, detailed inbox summary (~201 unread, correctly triaged as mostly promotional, one genuinely worth-a-look item surfaced: a GitHub unrecognized-location sign-in alert coinciding with a new Supabase OAuth authorization). Same permission fix as Calendar — added `search_threads`/`get_thread`/`get_message` to `.claude/settings.local.json`, confirmed via the exact tool name in a live error message before the fix. Level 1 in `permissions.ts`. Sends/drafts intentionally not built — real Level 3 surface for later | Cowork + Leonardo (live testing) |
| 16 | Web research | **Done, project-linking gap closed 2026-08-11** — `research <topic>` routes through the orchestrator, which writes findings to a new `Notes/` file and replies with a one-sentence summary + path. New `useActiveProject` hook (localStorage-persisted) + a "Set Active" control in `ProjectsView` let JARVIS know which project is the current focus; when one's set, the research prompt asks Claude to name that project in the note and reply. 3 Rust + 6 frontend tests | Local Claude Code + Cowork |
| 17 | Specialist agents | **Reframed and largely done, 2026-08-09** — see `AGENT_SYSTEM.md`'s rewrite: rather than separate agent processes/personas, "specialists" are four purpose-built prompt templates over the one `run_orchestrator` command (`research`, `continue-project`, `check-calendar`, `check-email`), each explicit about what it should and shouldn't do. Simpler than the original routing-table design and equally real. What's not built: the Agents status page (spec §42). The background-mode path (`claude --bg`) it was waiting on now exists (M10, 2026-08-10) — but only one command (`continue-project`) uses it, and the Command Log already surfaces its start/finish, so there's still not enough distinct concurrent background state to justify a dedicated status page. Revisit if more commands move to background mode | Cowork |
| 18 | Permissions + security | **Done, verified 2026-08-09** — `src/permissions.ts` classifies command kinds into Level 1/2/3 per spec §54. Level 3 (`delete-project`) is enforced with a real `ApprovalDialog` (ACTION/CONTEXT/REASON/RISK per spec §55) and `useApproval` promise-based hook — nothing deletes until the user clicks Approve, every time, no auto-approve. Wired into `ProjectsView`'s Delete button (the actual gated action) and recognized (not executed) by the typed command bar, which honestly redirects to the UI instead of faking a confirmation flow in text. 16 tests passing, build verified | Cowork |
| 19 | Testing + performance | **Substantially real, command latency now measured 2026-08-11.** All 5 orchestrator commands confirmed live in the actual app (M7/M10/M12/M14/M15/M16), and `scripts/benchmark_orchestrator.sh` produced real numbers: synchronous commands (research/calendar/email/GitHub) take 12–44 seconds each; continue-project's background-mode launch is consistently ~1s, with the job itself finishing in 5–13s. **Real finding:** that's a long silent wait for a voice/command-bar interaction with zero incremental feedback today — worth a UX pass before V1 polish is called done (see `TASKS.md`). `check-github` also showed notably more run-to-run variance (25s/25s/44s) than the others, worth a second look. App startup timing deliberately still not benchmarked — that's about the bundled release build, blocked on the known `PATH`-resolution gap in `orchestrator.rs` | Cowork + Leonardo (live run) |
| 20 | V1 polish | **Not started as its own pass, but most of what it would cover has already happened incrementally, 2026-08-11.** M7/M9/M10/M12/M14/M15/M16 are all confirmed live; M6 (Obsidian) is now reachable from the app for the first time. What's left: one exact-phrase confirmation (`continue project Ape War Game` — see `TASKS.md`), the bundled-app `PATH`-resolution fix (see `HANDOFF_PATH_FIX_AND_VERIFICATION.md`, needs `cargo`), and a real look at whether the interim "still working" feedback (added 2026-08-11) actually fixes the long-silent-wait UX finding from M19's benchmark once it's been seen live rather than just shipped. Most of the sidebar's remaining `NotBuiltView` placeholders (Agents, Automations, Research, Integrations, Activity, Notifications, System, Settings) are still genuinely not built — Memory was the one Leonardo named as missing, not a signal the rest should be rushed too | — |

| 21 | Conversations/messages persistence (Chat backbone) | **Built, 2026-08-11 — needs Leonardo to run the migration before it's live against Supabase.** New `Conversation`/`Message` types and 4 `JarvisStore` methods, implemented in `LocalStore` and `SupabaseStore` alike; `packages/database/migrations/0002_chat.sql` adds the tables. `useCurrentConversation` + `App.tsx` wiring persist every command-bar/voice exchange, fire-and-forget. Dashboard's Command Log UI intentionally untouched — Chat UI is M22. 46 tests passing, `tsc -b`/`vite build` clean. See `CHAT_MEMORY_SKILLS_CONNECTIONS_PLAN.md` for the full plan | Cowork |
| 22 | Chat UI (real tab, persisted history) | **Built, 2026-08-11.** New `Chat` sidebar item + `ChatView.tsx` — conversation switcher, full persisted thread (not capped at 6 like the Dashboard's Command Log), input reusing `handleCommand` unchanged (same parsing/permissions/orchestrator calls as command bar and voice). `App.tsx` gained `messages`/`conversations` state and `useCurrentConversation`'s new `selectConversation` setter. Depends on M21's migration actually being run for Supabase-backed persistence; works against `LocalStore` today regardless. `tsc -b`/`npm run test`/`vite build` all clean | Cowork |
| 23 | Connections registry (data + status UI) | **Built, 2026-08-11 — needs Leonardo to run the migration for Supabase-backed status.** New `ConnectionsView.tsx` (replaces "Integrations" placeholder): read-only registry of calendar/gmail/github/obsidian/web/supabase, live status computed the same honest way `StatusPanel` already does (never read from the DB — see new shared `useInTauri.ts`), capabilities shown with a read-only/write flag. `packages/database/migrations/0003_connections.sql` adds and seeds `connections`/`connection_capabilities` — identity/capability metadata only, never credentials. `LocalStore` returns a fixed built-in list (`lib/store/builtinConnections.ts`); `SupabaseStore` queries the real tables. 48 tests passing, `tsc -b`/`vite build` clean | Cowork |
| 24 | Skills data model | **Built, 2026-08-11 — deliberately scoped down from the plan's original wording (see `lib/store/builtinSkills.ts`'s doc comment and `TASKS.md`).** A descriptive registry (`skills`/`skill_connections`, seeded from the six existing commands with matching permission levels and declared Connections) alongside `commandEngine.ts`, not a rewrite of it — avoids the exact regression risk the plan flagged for this milestone. New regression test ties `builtinSkills.ts`'s permission levels to `permissions.ts` directly. `packages/database/migrations/0004_skills.sql` (run after 0003). 51 tests passing, `tsc -b`/`vite build` clean. No UI yet — Milestone 25 | Cowork |
| 25 | Skills UI + manual invocation | **Plan written, not started** | — |
| 26 | skill_runs / activity_events logging extension | **Plan written, not started** | — |
| 27 | Memory index (real vault browsing) | **Plan written, not started — needs cargo, will hand off to local Claude Code** | — |
| 28 | Skill-aware permission enforcement in UI | **Plan written, not started** | — |
| 29 | Voice ↔ Chat integration polish | **Plan written, not started** | — |

## Milestones 21–29: Chat + Memory + Skills + Connections

New scope Leonardo requested 2026-08-11 (pasted spec, chat/memory/skills tabs instead of one-shot
orders). Full structured plan — current state, reusable components, database/UI/voice changes,
security considerations, dependency-ordered milestone table — is in
`CHAT_MEMORY_SKILLS_CONNECTIONS_PLAN.md`. Per the spec's own instruction, nothing above is
implemented yet; this table exists so the new milestones are tracked the same honest way as 1–20
rather than living only in a separate doc. Awaiting Leonardo's go-ahead on the plan before M21
starts.

## Sequencing note

Milestones 1–2 are done (2026-08-09). Stack is decided: Tauri, Supabase Cloud, no n8n initially
(launchd/cron scripts instead), local Claude Code as orchestrator, vault at
`~/Documents/Obsidian Vault` — see `ARCHITECTURE.md`. Before Milestone 3 starts: relocate this
repo out of the Claude session artifacts folder to somewhere durable (e.g. `~/Developer/jarvis`),
and recheck disk space (~22 GiB free at inspection time) since the Rust toolchain install is
next.
