# Roadmap

Full milestone-by-milestone history (M1–M29, 2026-08-09 to 2026-08-11) is archived at
[`docs/archive/ROADMAP_M1-29.md`](docs/archive/ROADMAP_M1-29.md) — real bugs, real fixes, real
verification notes, worth keeping but too long to stay the working doc. This file is the
current-state summary only.

## Where things stand (2026-08-13)

**Built and live-confirmed:** desktop app shell (Tauri + React) with a top-nav layout (M35,
replaced the old 13-item left Sidebar) and an Iron-Man-HUD visual system (M34: bracket-corner
panels, glow, grid backdrop, restyled `JarvisCore`) across all 4 themes, command engine (typed +
voice, same parser), local orchestrator (shells out to `claude` CLI), Supabase-backed
Projects/Tasks/Chat/Connections/Skills/Activity, Obsidian vault read access, voice (wake word →
transcribe → speak, crash-recovery, main window only), calendar/email/GitHub read-only checks,
Level 1/2/3 permission classification with a real approval dialog for Level 3, launchd-based
(not n8n) scheduled morning brief (written, not activated), multi-window pop-out (any real view
opens in its own Tauri window, verified both frontend and Rust — M32), a real System overview
(M36: version/build/runtime, permission-level summary, Connections/Activity summary tiles, honest
empty Devices panel), a double-clickable Desktop launcher (`~/Desktop/Jarvis.app`, see
`docs/DESKTOP_LAUNCHER.md` — tooling, not a numbered milestone).

**Voice, 2026-08-13:** two real self-listening bugs found and fixed (a permanent-deafness gap in
`listen_loop.py`'s staleness check, and no guard against a second app instance forming its own
independent voice-process pair) — see `VOICE_SETUP.md`'s dated section. Both compiled/syntax-
checked but not yet exercised with a real mic. Separately, closing the gap between our old
turn-based pipeline and a Gemini-Live-style real-time conversation (M40, below) — a new
`gemini_live` engine, selectable per-user, coexisting with the unchanged classic pipeline.

**Target end-state:** `PROJECT_OBJECTIVE.md` (2026-08-11) is Leonardo's own description of what
JARVIS should actually become, walked through against three visual references. Its two
governing rules for everything below: every panel migrates toward the Iron-Man-HUD visual style
in those references, and **every number or status shown must be real — no decorative sci-fi
metrics, no fictional subsystem names.** Milestones 34-39 below are that vision broken into
buildable pieces, in the order decided during that walkthrough.

**Explicitly not built, not started, not stubbed:** anything hardware (3D printer, robotic arm
— no driver, no live Skill, no mention in real device code; M31 added a `domain` field so a
hardware Skill has a real slot, System's Devices panel is ready to show one, nothing more exists
yet), school/study tracking, write actions for calendar/email/GitHub (intentionally deferred —
see `docs/SECURITY.md`), Quick Actions on System (intentionally omitted — no real backend action
exists yet to back one).

**Known live gaps carried forward:** Memory index / real vault browsing (M27/M33, needs `cargo`,
see [`docs/archive/HANDOFF_MEMORY_INDEX_AND_MIGRATIONS.md`](docs/archive/HANDOFF_MEMORY_INDEX_AND_MIGRATIONS.md)),
bundled-app `PATH` resolution in `orchestrator.rs` (works under `cargo tauri dev`, untested in a
double-clicked release build — see
[`docs/archive/HANDOFF_PATH_FIX_AND_VERIFICATION.md`](docs/archive/HANDOFF_PATH_FIX_AND_VERIFICATION.md)),
M32's `core:default` permission grant on popped-out windows (structurally correct, not yet
behaviorally exercised — no code calls `event:listen` from one to test it) and multi-monitor
placement (untested, single-display dev machine), M37's Rust half (needs `cargo`, higher risk
than usual — see [`docs/archive/HANDOFF_M37_SYSTEM_STATS_RUST_VERIFICATION.md`](docs/archive/HANDOFF_M37_SYSTEM_STATS_RUST_VERIFICATION.md)),
M38's live end-to-end verification (needs a real Tauri session with an unpredictable orchestrator
response, see M38's row above).

## Current milestones

| # | Milestone | Status |
|---|---|---|
| 30 | Docs restructure | **Done, 2026-08-11** |
| 31 | Skills engine redesign | **Done, 2026-08-11** — real `Skill` interface (`skills/types.ts`: permission level, `domain: software \| hardware`, `execute()`) and registry (`skills/registry.ts`) now the single source of truth for the six built-in Skills' prompts, permission levels, and execution — previously duplicated by hand across `commandEngine.ts`'s switch statement, `permissions.ts`, and `lib/store/builtinSkills.ts`. All three now derive from the registry. No hardware Skill added (no device to back one) — `domain` just gives one a real place to slot in later. 57 tests passing (up from 53), `tsc -b`/`vite build` clean, no behavior change |
| 32 | Multi-window foundation | **Done, live-verified end to end, 2026-08-11.** Any real view (Dashboard/Chat/Projects/Tasks/Memory/Skills/Connections/Activity) can pop out into its own Tauri window via a nav button (originally the Sidebar, moved to the top nav at M35) — `?view=<slug>` loads the same bundle standalone, no shared cross-window state sync (each window independently reads the same store). Voice listener deliberately disabled outside the main window (one global backend process, not per-window). Rust half (`windows.rs`, `main.rs` registration, `capabilities/default.json`'s `"view-*"` glob) compiles clean and was click-through verified live: real second OS window, correct title/content/no-nav, focus-not-duplicate on repeat click. Two things genuinely still unverified, not swept under the rug: `core:default` permission grant on popped-out windows and multi-monitor placement (see "Known live gaps" above). See [`docs/archive/HANDOFF_M32_MULTIWINDOW_RUST_VERIFICATION.md`](docs/archive/HANDOFF_M32_MULTIWINDOW_RUST_VERIFICATION.md) for full verification detail |
| 33 | Memory index (carried over from M27) | Not started — needs `cargo`, real vault filesystem scanning. `PROJECT_OBJECTIVE.md`'s reference 2 (Memory Core) now gives this a concrete UI target: category counts, storage stats, linked-memory detail panel — all requiring a real vault index, not just today's single summarize-on-request prompt |
| 34 | Visual design system: tokens + `JarvisCore` restyle | **Done, live-verified across all 4 themes, 2026-08-11.** `.panel` (shared by every view — Dashboard/Projects/Tasks/Memory/Chat/Connections/Skills/Activity/StatusPanel/VoiceSettings) restyled with bracket-corner accents, an angular radius, and an accent glow — one CSS change, whole app picked it up with zero per-view edits. Added a faint theme-driven grid backdrop to `body`. `JarvisCore`'s SVG extended (not replaced) with 24 fixed instrument-bezel ticks and a vertical light beam through the core, alongside the existing 3-ring/nucleus animation system — all 10 states (idle through error) still read correctly through the additions. `status-connected` badges got a small glow, kept deliberately off unverified/not-wired states so the glow stays an honest signal. 57 tests passing (unchanged — CSS/SVG only), `tsc -b`/`vite build` clean, checked live in a browser preview across Holographic Core, Crimson Command, and Neon Void |
| 35 | Top-nav migration | **Done, live-verified, 2026-08-11.** `Sidebar.tsx` (13 items, 5 real placeholders like Research/Automations/Notifications never backed by anything) replaced by `TopNav.tsx`: a flat 9-item bar — Dashboard, Chat, Skills, Memory, Connections, Projects, Tasks, Activity, System — every item real except System (included because all three references confirm it as primary; shows an honest `NotBuiltView` until M36). Decided against a "more" dropdown for the non-primary-5 views (Projects/Tasks/Connections/Activity) — nothing lost, no extra interaction layer; can revisit if the bar feels crowded. Renamed two mismatched labels while migrating: "Agents"→"Skills" and "Integrations"→"Connections" (both always rendered `SkillsView`/`ConnectionsView`; only the label was wrong). M32's pop-out buttons moved into `TopNav` alongside each item. `.app-shell` changed from a 2-column grid to a flex column (nav row + scrollable main). 57 tests passing (unchanged), `tsc -b`/`vite build` clean, live-checked: routing to all 9 items, System's honest not-built state, and `?view=skills` standalone rendering with the renamed slug |
| 36 | System/Settings view — software slice | **Done, live-verified, 2026-08-11 — scope refined during build.** New `SystemView.tsx` replaces the `System` placeholder: real App info (version, dev/prod build mode via `import.meta.env.DEV`, Tauri-vs-browser runtime — all genuinely detected, not hardcoded), real Permissions summary (Skill counts by Level 1/2/3, derived from `skills/registry.ts`), a Connections summary tile (real count + "Open Connections" link) and an Activity summary tile (last 3 real events + "Open Activity" link), and an honest empty-state Devices panel (idea #3's home, stays empty until a real hardware Skill exists). **Deliberately does not re-embed full `ConnectionsView`/`ActivityView` content** — both already have their own top-nav tab since M35, so duplicating full lists here would be the "add stuff, don't overload it" mistake Leonardo flagged reviewing the visual references; summary tiles that link out instead, matching the references' own "OPEN SECURITY CENTER" pattern. **No Quick Actions section** — every action in the references (Optimize System, Clear Cache, Restart Core) would need a real backend call behind it and none exists yet; adding non-functional buttons would violate the real-data rule worse than omitting the section. `System` added to `popoutViews.ts` now that it's real content. 57 tests passing (unchanged), `tsc -b`/`vite build` clean, live-verified: real Skill/Connection counts confirmed correct against the registries, honest "Browser preview" runtime detection, both navigate-out links work |
| 37 | System/Settings view — real performance stats | **Done, compiled clean, 2026-08-12.** Frontend as before (Performance panel in `SystemView.tsx`, honest 3-state degradation). Rust half (`system_stats.rs`, `sysinfo` crate) turned out to be verifiable after all — see the session note below on `cargo` actually being reachable. `cargo add sysinfo` resolved `0.39.6`; `cargo build` succeeded on the first try, no fixes needed to the hand-written API usage. Real running app relaunched clean afterward (`target/debug/jarvis` up, no compile errors). Live confirmation of the actual on-screen numbers (vs. Activity Monitor) still needs a look at the real window — see `project/TASKS.md` |
| 38 | "Do anything it has access to" — real action path | **Built, unit-tested, 2026-08-11 — real live end-to-end verification still open.** `ask` (the fallback for anything not matching a built-in Skill) now has a two-call design instead of permanent describe-only: first asks the orchestrator to classify its own reply `SAFE:` (read/analyze/explain, answer immediately) or `NEEDS_APPROVAL:` (would change something, describe the plan and stop). A `NEEDS_APPROVAL` plan routes through the exact same `ApprovalDialog`/`useApproval` flow the six built-in Skills' Level 3 case uses (a new `requestApproval` field on `CommandContext`, wired from `App.tsx`); on approval, a second independent prompt tells the orchestrator to actually do it. Deliberately binary, not Level 1/2/3 — for arbitrary free text with no pre-reviewed prompt template, treating any real change as needing approval is the safer default. **Honesty caveat, not hidden:** this is a behavioral guardrail via prompting, not a technical sandbox — nothing stops the model from acting before classifying, the same limitation the old always-describe prompt had. **Genuinely not yet verified:** whether the real `claude` CLI reliably follows the `SAFE:`/`NEEDS_APPROVAL:` convention in practice, and whether the approval dialog fires correctly end-to-end inside the actual running app — this needs a live Tauri session with an unpredictable real response, which a browser preview can't exercise (`invoke` fails outside Tauri). 4 new tests (61 total, up from 57) cover the gating logic itself (approve/deny/no-approval-flow available/format-fallback), `tsc -b`/`vite build` clean. Suggested live test phrase once run locally: something unambiguously action-requiring, e.g. "delete my notes.txt file" |
| 39 | Self-upgrade skill | **Built and live-verified, 2026-08-11.** New `self-upgrade` Skill in `skills/registry.ts` (Level 3, unlike `continue-project`'s Level 2 — a bad change here can break the running app, not just an external project), triggered by "upgrade yourself" / "update yourself" with an optional `: <focus>` — if omitted, the prompt tells the orchestrator to read `project/ROADMAP.md`/`project/TASKS.md` and pick the next sensible item itself. Reuses `continue-project`'s background-mode-with-sync-fallback pattern exactly. **Zero new gating code needed** — M28's generic Level-3 gate and M31's registry mean adding a Level 3 Skill is now just a registry entry; `permissions.ts` and `App.tsx`'s `SKILL_COMMAND_KINDS` picked it up automatically. Live-verified against the actual running desktop launcher instance: typing "upgrade yourself" correctly triggered the real `ApprovalDialog` with accurate Level 3 context, and denying it produced "Not approved — nothing was run" with no orchestrator call attempted. **Real gap found and fixed during verification:** this app's live instance runs against real Supabase (not the `LocalStore` fallback), and Supabase's `skills` table is seeded once via migration SQL, not read from `skills/registry.ts` — the new Skill didn't appear in the live Skills tab until this was caught. New `packages/database/migrations/0006_self_upgrade_skill.sql` fixes it; **Leonardo needs to run this in the Supabase SQL editor**, same as every prior migration, before the Skills tab shows all 7. SkillsView.tsx updated for an optional (not required) focus input. 6 new tests (67 total, up from 61), `tsc -b`/`vite build` clean |
| 40 | Real-time conversational voice (Gemini Live) | **First slice built 2026-08-13, five real bugs found and fixed from live testing through 2026-08-14 — still not confirmed fully stable.** New `voice_engine` config option: `"classic"` (unchanged) or `"gemini_live"` (new `gemini_live_listen.py`, System/voice/ in the Obsidian vault). Wake word still gates when a live session opens; session stays open for natural back-and-forth until a silence timeout or an end phrase. One process handles both mic capture and speaker playback. `voice.rs` extended (not duplicated) to spawn either engine; `VoiceEvent` gained 3 new variants. Frontend: "Conversation engine" selector, live transcripts persisted to Chat. Fixed live: blocking I/O freezing the event loop, an over-eager end-phrase substring match, `session.receive()` only handling one turn, VAD/wake-word oversensitivity, and (2026-08-14) a Spanish-language switching bug — root-caused to a native-audio model's language behavior via a full step-back design review (`docs/VOICE_GEMINI_LIVE_REVIEW_2026-08-14.md`), fixed with a `system_instruction`. **Deliberately scoped out:** no tool-calling bridge to `commandEngine.ts` yet. **Verified:** real `cargo build`/`cargo test` (24 Rust tests), 67 frontend tests, `tsc -b`/`vite build` clean, UI live-checked. Every Python-side fix is syntax-checked only pending Leonardo's next live test. Full detail: `VOICE_SETUP.md`'s dated sections |
| — | Native AEC bridge for Gemini Live (`aec_bridge/`) | **Built 2026-08-14**, not yet live-tested end to end. Small standalone Swift binary (`System/voice/aec_bridge/main.swift`, built via `build.sh`/`swiftc`, no Xcode project) using `AVAudioEngine`'s `VoiceProcessingIO` for real acoustic echo cancellation — unreachable from Python's audio libraries, only from native code. Opt-in via `config.json`'s new `audio_backend` field (`"sounddevice"` default/unchanged, or `"aec_bridge"`); `gemini_live_listen.py` gained an `AecBridgeAudio` class talking to it over stdin/stdout. Fixed three real CoreAudio -10875 engine-start failures during development (voice processing needed on both input and output nodes; tap format must be queried after enabling it) by isolating causes in minimal test binaries against this machine's real hardware — the engine now starts cleanly and negotiates real device formats. **Not verified:** actual captured audio end-to-end — the environment that built this runs under the Claude Code app's process tree, not a real interactive Terminal, so it can't get real microphone TCC access to prove that part; needs Leonardo's own live test. See `project/TASKS.md`/`VOICE_SETUP.md` |
| 41 | Gemini Live tool-calling bridge | **Built 2026-08-14, all three languages compile/test clean — not yet live-tested with real audio.** One generic Gemini function tool (`run_jarvis_command`, taking free text — the same shape a person would type into the command bar), not one tool per feature. Real end-to-end IPC, new both directions: `gemini_live_listen.py` gained `ToolBridge` (emits `{"event":"tool_call",...}` on stdout, awaits a matching JSON result on stdin — decoupled from the per-wake-word-cycle event loop `asyncio.run()` recreates each time, via a process-wide `queue.Queue` drained by a fresh per-session task, not a loop-bound `asyncio.Future` captured too early); `voice.rs` now pipes the listener's stdin (previously unused) and gained `send_voice_tool_result`, a new `VoiceEvent::ToolCall` variant, and `VoiceState.listener_stdin`; the frontend gained `runVoiceCommand`/`runVoiceCommandConfirmed` in `commandEngine.ts` and an `onToolCall` handler in `App.tsx`. Both new frontend functions route through the *same* `executeCommand` path everything else uses (typed commands, classic voice, "ask") — not a parallel execution system. **Level 3 confirmation is voice-native, not a popup, by explicit design decision:** a Level 3 Skill or a NEEDS_APPROVAL `ask` never opens the real `ApprovalDialog` from voice — instead the bridge tells Gemini a spoken confirmation question, and the *next* turn's actual go/no-go is decided by `gemini_live_listen.py`'s own deterministic near-exact match against yes/no phrasing (`is_confirmation_yes`/`is_confirmation_no`, mirroring `is_end_phrase()`'s existing pattern) — never by trusting Gemini's own judgment that it heard a "yes". Confirmed execution is delivered back into the conversation via `session.send_client_content()` (a real, confirmed-real SDK method, checked directly against the installed SDK the same way every other Live API detail was this session), prompting Gemini to speak the actual result. **Verified:** real `cargo build`/`cargo test` (25 Rust tests, 1 new), 73 frontend tests (6 new, covering both the Level-3-Skill and the "ask" NEEDS_APPROVAL paths on both the discovery and confirmed passes), `tsc -b`/`vite build` clean, Python syntax-checked. **Not yet verified:** an actual spoken tool call end to end — needs Leonardo's own live test once the app rebuilds with these changes. See `VOICE_SETUP.md`'s 2026-08-14 section |
| 42 | Native Rust rewrite of the Gemini Live voice path | **Scoped 2026-08-15, dependency spike verified, implementation not started.** Real decision, not a patch: after five real bugs found live-testing M40/M41, all of them at process/language boundaries (Rust↔Python↔Swift, each with a hand-rolled JSON-over-stdio protocol), Leonardo decided to eliminate the boundaries rather than keep patching them — move the entire Gemini Live + AEC path in-process into the Rust backend, replacing `gemini_live_listen.py` and `aec_bridge/` (Swift) entirely. **Scope, explicit:** only the Gemini Live engine + AEC. The classic engine (`listen_loop.py`/`speak_daemon.py`, wake→record→Whisper→speak) is untouched — it hasn't had reported problems. **Real research done before committing, not guessed:** Gemini Live's WebSocket wire protocol is officially documented (`wss://generativelanguage.googleapis.com/.../BidiGenerateContent`, JSON envelope with `setup`/`realtimeInput`/`toolCall`/`toolResponse`) — reimplementable directly, no official Rust SDK needed, `tokio-tungstenite` is the client. Wake word: switching from openWakeWord to `livekit-wakeword` (Leonardo's choice, 2026-08-15) — Rust-native and actively maintained vs. hand-porting openWakeWord's mel-spectrogram preprocessing, which nothing production-ready wraps in Rust yet; means "Hey Jarvis" needs reconfiguring against the new model, a real known tradeoff, not hidden. AEC: `objc2-avf-audio` gives near-1:1 access to the same `AVAudioEngine`/`setVoiceProcessingEnabled` API the working Swift `aec_bridge` already proved out — the closest-to-direct port of the three pieces. **Verified 2026-08-15:** the real, once-uncertain risk — whether these three crates plus `tokio` even resolve and compile together in this actual project — is cleared. `cargo add objc2-avf-audio livekit-wakeword tokio tokio-tungstenite` (with `rustls-tls-webpki-roots` for the required `wss://` TLS) resolved cleanly (one real conflict found and fixed along the way: a redundant direct `ort` dependency clashed with `livekit-wakeword`'s own bundled `ort-tract` pin), and `cargo build`/`cargo test` succeeded with all 25 existing Rust tests still passing. **WebSocket handshake spike succeeded, 2026-08-15** (`src/bin/gemini_live_spike.rs`, throwaway/not part of the app): connected to the real Gemini Live endpoint from Rust, sent `setup`, received a genuine `setupComplete` — the first real proof this protocol works from Rust, not just that it's documented. Two real, non-obvious findings from actually running it, not guessed from docs: (1) rustls 0.23 needs an explicit crypto backend installed at process start (`rustls::crypto::ring::default_provider().install_default()`) or every connection panics; (2) **Gemini Live sends JSON as binary WebSocket frames, not text frames** — `ai.google.dev`'s docs don't make this obvious, and code that only checks `Message::Text` (the natural first guess) silently never sees any server response. Both fixed and confirmed in the spike; both need carrying into the real implementation. Main app build/tests unaffected (still 25 Rust tests passing) with the new dependencies and spike binary present.

**Not yet done:** the real pipeline. No `realtimeInput`/audio streaming, no `toolCall` handling, no wake-word integration, no AEC wiring, no replacement of `voice.rs`'s Python-spawning path — only the initial handshake is proven. This is a genuine multi-session rewrite, not a quick milestone. See `docs/VOICE.md` for the current (pre-rewrite) architecture this replaces |
| — | School/study tracking | Not scoped yet — `[daily-life]` backlog item, see `project/TASKS.md` |
| — | Hardware device connections + first hardware Skill | Not scoped yet — blocked on Leonardo naming a real reachable device (3D printer, robotic arm, or other); M31's `Skill` interface and M36's Devices panel are both ready for one whenever that happens |

## Working agreement going forward

Lightweight process (per Leonardo, 2026-08-11): one short spec note before a milestone starts,
a terse `CHANGELOG.md` entry when it's done, this table's status updated. No approval-gate
ceremony beyond that — see `VISION.md` for why (single user, not a team).
